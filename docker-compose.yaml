
services:
    java-maven-app:
      image: "onyebuchia/app-store:${IMAGE}"
      ports:
        - 8080:8080
    postgres:
      image: postgres:15
      ports:
        - 5432:5432
      environment:
        - POSTGRES_PASSWORD=my-pwd